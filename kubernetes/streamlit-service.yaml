apiVersion: v1
kind: Service

metadata:
  name: voyage-streamlit-service
  namespace: voyage-analytics

spec:
  type: NodePort

  selector:
    app: voyage-streamlit

  ports:
  - protocol: TCP
    port: 8501
    targetPort: 8501
    nodePort: 30081
